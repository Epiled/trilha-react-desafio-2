import styled from "styled-components";

export const ErrorMenssageStyled = styled.span`
  position: absolute;
  left: 0;
  top: calc(100% + .5rem);
  color: var(--error);
  font-size: 1.4rem;
  font-weight: bold;
`
